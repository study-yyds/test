<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >
    <!-- 添加通知权限（Android 13+ 必需） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 添加前台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- 添加前台服务数据同步权限（Android 14+ 必需） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">


        <!-- 添加 MainActivity 配置 -->
        <activity
            android:name="com.example.expensetracker.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- 新增：记账监控服务 -->
        <service
            android:name=".ExpenseMonitorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            />

        <!-- 注册ContentProvider -->
        <!-- 在<application>标签内添加 -->
        <provider
            android:name=".ExpenseProvider"
            android:authorities="com.example.expensetracker.provider"
            android:exported="false"
            android:enabled="true"/>

        <!-- 注册BroadcastReceiver -->
        <!-- 静态注册广播接收器（示例） -->
        <!-- 注意：Android 8.0+ 对静态注册有限制，这里主要用于教学演示 -->
        <receiver
            android:name=".ExpenseBroadcastReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.expensetracker.ACTION_TRANSACTION_ADDED" />
                <action android:name="com.example.expensetracker.ACTION_DATA_UPDATED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>